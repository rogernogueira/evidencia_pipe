"""Estágios 3/4 do pipeline: chunking, filtros de qualidade e indexação no Qdrant.

Movidos da raiz do projeto para dentro do pacote `backend` (antes eram módulos
top-level importados via hack de sys.path).
"""
