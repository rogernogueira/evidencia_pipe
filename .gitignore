# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment
.env

# Cache / dados / artefatos
.pytest_cache/
.ruff_cache/
data/
output/
qdrant_storage/

# Área de build/scratch: versiona só o Dockerfile do MinerU, ignora o resto (logs, runs).
tmp/*
!tmp/Dockerfile

# Sincronização DSpace → API (scripts/sincronizar_novos_itens.py)
logs/
estado_sincronizacao.json
.sincronizacao.lock
# Ledger do que já foi enviado (compartilhado com scripts/extrair_e_enviar_uuids.py):
# é estado de execução, muda a cada rodada.
itens_uuid.csv
